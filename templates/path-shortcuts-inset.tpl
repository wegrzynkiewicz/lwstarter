# Contain path shortcut allow fast changing current directories using qwecd
# Format [name]:[path]\n

lwc:${LW_STARTER_CONFIG_DIR}
lwp:${LW_STARTER_PROJECT_DIR}
lwf:${LW_STARTER_PROJECT_DIR}/entries/fixtures
